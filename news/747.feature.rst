Improve error reporting for capture-file failures: log the underlying ``errno`` (and a hint to try a different output location) when ``mmap`` or ``posix_fallocate`` fails inside ``FileSink``; append ``strerror`` to the ``listen`` failure in ``SocketSink``, to the ``/proc/self/status`` open failure in the background thread, and to the ``Failed to write output header`` ``RuntimeError`` so the cause is visible in Python tracebacks even without ``MEMRAY_LOG_LEVEL`` raised.
